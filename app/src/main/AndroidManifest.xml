<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.CAMERA" />

  <uses-feature
      android:name="android.hardware.camera"
      android:required="false" />

  <application
      android:name=".ShowcaseApplication"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.ShowcaseApp">
    <activity
        android:name=".ShowcaseActivity"
        android:exported="true"
        android:label="@string/showcase_activity_name"
        android:theme="@style/Theme.ShowcaseApp"
        android:launchMode="singleTop">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />

        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <!-- scheme should match redirect URI scheme configured on the backend -->
        <!-- scheme is also displayed as part of result using sdk configurator -->
        <data android:scheme="oneginiexample" />

      </intent-filter>
    </activity>
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
    </provider>
    <service
        android:name="com.onewelcome.showcaseapp.fcm.ShowcaseAppFirebaseMessagingService"
        android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>

    <!-- Hybrid Transport / Credential Provider Components -->

    <!--
        CredentialProviderService: Entry point for Android's Credential Manager.
        This service responds to CreateCredentialRequest or GetCredentialRequest
        and delegates the actual logic to the PasskeyHandlerActivity.
    -->
    <service
        android:name="com.thalesgroup.gemalto.fido2.ui.CredentialProviderService"
        android:enabled="true"
        android:exported="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:permission="android.permission.BIND_CREDENTIAL_PROVIDER_SERVICE"
        tools:targetApi="35">

      <intent-filter>
        <action android:name="android.service.credentials.CredentialProviderService" />
      </intent-filter>

      <!-- Provider capabilities configuration -->
      <meta-data
          android:name="android.credentials.provider"
          android:resource="@xml/provider" />

      <!-- FIDO provider configuration for account types -->
      <meta-data
          android:name="com.thalesgroup.gemalto.fido2ui.CONFIGURATION"
          android:resource="@xml/fido_provider_config" />
    </service>

    <!--
        PasskeyHandlerActivity: UI component responsible for handling user interaction
        during the passkey ceremony.
    -->
    <activity
        android:name="com.thalesgroup.gemalto.fido2.ui.PasskeyHandlerActivity"
        android:exported="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:theme"
        tools:targetApi="35">

      <!-- Intent filter for passkey creation (registration) -->
      <intent-filter>
        <action android:name="com.thalesgroup.gemalto.fido2ui.action.CREATE_PASSKEY" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>

      <!-- Intent filter for passkey authentication (login) -->
      <intent-filter>
        <action android:name="com.thalesgroup.gemalto.fido2ui.action.GET_PASSKEY" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
      </intent-filter>
    </activity>


  </application>
</manifest>
