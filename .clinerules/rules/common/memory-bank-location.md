# Memory Bank Location

**Rule:** The project's Memory Bank (context documentation) is located in the `ai/memory_bank` directory.

## Instructions for Cline
- When starting a task or needing context, always read files from `ai/memory_bank/`.
- The active context file is `ai/memory_bank/activeContext.md`.
- Project progress is tracked in `ai/memory_bank/progress.md`.
